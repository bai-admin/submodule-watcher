import path from "path";

export const MODULE_GLOB       = path.join("convex", "*");
export const ROOT_SCHEMA_FILE  = path.join("convex", "schema.ts");
export const ROOT_HTTP_FILE    = path.join("convex", "http.ts");
export const ROOT_CONFIG_FILE  = path.join("convex", "convex.config.ts");
export const MERGE_ROUTER_FILE = path.join("convex", "router.ts");
